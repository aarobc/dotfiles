# source = ~/.config/hypr/monitors.conf
source = ~/.config/hypr/autostart.conf
source = ~/.config/hypr/input.conf
source = ~/.config/hypr/plugin.conf
source = ~/.config/hypr/custom/*



# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal    = foot
$fileManager = thunar
$menu        = wofi --show run --insensitive --matching strict-contains


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = QT_AUTO_SCREEN_SCALE_FACTOR,0
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_QPA_PLATFORMTHEME,qt6ct
env = GTK_THEME,Adwaita:dark
env = _JAVA_AWT_WM_NONREPARENTING,0
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = XDG_SESSION_TYPE,wayland

###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 20

    border_size = 2

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = true

    layout = hy3
    # layout = dwindle
    # layout = master
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes,

    # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    # Default animations, see https://wiki.hypr.land/Configuring/Animations/
    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     2,     default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     2,  easeOutQuint
    animation = windowsIn,     1,     2,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     2,     easeOutQuint, fade # starting up apps speed
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     3,     quick
}



# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
    permanent_direction_override = true
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}

cursor {
    # no_warps = false
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mod = SUPER # Sets "Windows" key as main modifier

$apos   = code:24
$comma  = code:25
$period = code:26
$p      = code:27
$r      = code:32
$l      = code:33

$o = code:39
$e = code:40
$u = code:41
$d = code:43
$h = code:44
$t = code:45
$n = code:46
$s = code:47
$j = code:54
$k = code:55

# lock

bind = $mod, $l, exec, hyprlock

# Example binds,   see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mod,       Return, exec, $terminal
bind = $mod SHIFT, $apos,  killactive,
bind = $mod SHIFT, M,      exit, # same on both
bind = $mod SHIFT, space,  togglefloating,
bind = $mod,       $e,     exec, $menu
# bind = $mod,     P,      pseudo, # dwindle
bind = $mod,       $period,      togglesplit, # dwindle

bind = $mod, $d, layoutmsg, preselect r
bind = $mod, $k, layoutmsg, preselect d

# hy3 splits
bind = $mod, $d, hy3:makegroup, h
bind = $mod, $k, hy3:makegroup, v

# hy3 plugin: focus parent node (like mod+a in i3)
bind = $mod, A, hy3:changefocus, raise

bind=$mod, $u, fullscreen

$movefocus = hy3:movefocus
$movewindow = hy3:movewindow
$movetoworkspace = hy3:movetoworkspace

# $movefocus = movefocus
# $movewindow = movewindow
# $movetoworkspace = movetoworkspace

# Move focus with mainMod + arrow keys
bind = $mod, $h, $movefocus, l
bind = $mod, $s, $movefocus, r
bind = $mod, $n, $movefocus, u
bind = $mod, $t, $movefocus, d

# move window
bind = $mod SHIFT, $h, $movewindow, l
bind = $mod SHIFT, $s, $movewindow, r
bind = $mod SHIFT, $n, $movewindow, u
bind = $mod SHIFT, $t, $movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

bind = $mod SHIFT control, $h, movecurrentworkspacetomonitor, l
bind = $mod SHIFT control, $s, movecurrentworkspacetomonitor, r

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mod SHIFT, 1, $movetoworkspace, 1
bind = $mod SHIFT, 2, $movetoworkspace, 2
bind = $mod SHIFT, 3, $movetoworkspace, 3
bind = $mod SHIFT, 4, $movetoworkspace, 4
bind = $mod SHIFT, 5, $movetoworkspace, 5
bind = $mod SHIFT, 6, $movetoworkspace, 6
bind = $mod SHIFT, 7, $movetoworkspace, 7
bind = $mod SHIFT, 8, $movetoworkspace, 8
bind = $mod SHIFT, 9, $movetoworkspace, 9
bind = $mod SHIFT, 0, $movetoworkspace, 10

# Example special workspace (scratchpad)
# bind = $mod, S, togglespecialworkspace, magic
# bind = $mod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
# bind = $mod, mouse_down, workspace, e-1
# bind = $mod, mouse_up,   workspace, e+1

bind = $mod, mouse_down, exec, ~/dotfiles/waybar/scroll.sh 0
bind = $mod, mouse_up,   exec, ~/dotfiles/waybar/scroll.sh 1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppress_event maximize, match:class .*

windowrule = float on, match:class 1password, match:initial_title 1Password
# Fix some dragging issues with XWayland
windowrule = no_focus on, match:class ^$, match:title ^$, match:xwayland 1, match:float 1, match:fullscreen 0, match:pin 0

# maybe to fix vr stuff:
windowrule = immediate on, match:class ^(steamvr)$
windowrule = immediate on, match:class ^(vrcompositor)$
windowrule = immediate on, match:class ^(vrmonitor)$

# jetbrains stuff
windowrule = tag +jbw, match:class ^(jetbrains-.*)$, match:title ^$, match:float 1
windowrule = no_anim on, match:tag jbw
# windowrule = stay_focused on, match:tag jbw

windowrule = float 1, match:class ^(nwg-displays)$
windowrule = float 1, match:class ^(gamescope)$


windowrule = stay_focused on, match:class ^gcr-prompter$, match:title ^Unlock Login Keyring$, match:float 1

# mouse_refocus = false
# cursor {
#     no_hardware_cursors = true
# }
